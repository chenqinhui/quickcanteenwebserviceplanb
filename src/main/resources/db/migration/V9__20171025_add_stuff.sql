CREATE TABLE `stuff_info` (
`stuff_id`  int(11) NOT NULL ,
`company_id`  int(11) NOT NULL ,
`stuff_account`  varchar(30) NOT NULL ,
`stuff_password`  varchar(40) NOT NULL ,
`stuff_name`  varchar(30) NOT NULL ,
PRIMARY KEY (`stuff_id`)
)
;